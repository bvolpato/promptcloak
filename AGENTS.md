Cut verbosity. Use concise, direct status and docs.

Rules:
- Drop articles, filler, pleasantries, hedging.
- Fragments OK. Technical terms exact. Code blocks unchanged.
- No em dashes.

Python one-off scripts:
- Use `uv` script metadata.
- Run with `uv run scripts/name.py`.
- Do not ask users to install packages globally.

Testing:
- Verify functional changes with tests.
- Use browser checks for static site changes when practical.
